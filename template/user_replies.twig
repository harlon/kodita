{% include 'header.twig' %}

<div class="user_activity">

    {% for comment in replies %}
        
        <div>
            {{ comment.text|slice(0, 256)|markdown|raw }}
            
            {# Post info #}
            <div class="info">
                <a href="../post/{{ comment.postHashId }}#comment-{{ comment.hashId }}">leer</a>
                <a href="../reply?comment={{ comment.hashId }}">respondido</a>
                —
                por <a href="../user/{{ comment.username|url_encode }}">{{ comment.username }}</a> <time title="{{ comment.created|title }}" datetime="{{ comment.created|datetime }}"><em> {{ comment.created|ago }} </em></time> on <a href="../post/{{ comment.postHashId }}">{{ comment.postTitle }}</a>
            </div>
        </div>
        
    {% endfor %}

</div>
        
{% include 'footer.twig' %}
